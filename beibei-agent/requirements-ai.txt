# ============================================================================
#  背备不悲 · beibei-agent 重依赖（M1 阶段再装，约 4~6 GB）
#  安装：  D:\conda-envs\beibei\python.exe -m pip install -r requirements-ai.txt
#
#  ⚠️ 必须用 D:\conda-envs\beibei（Python 3.11），不要用系统自带的 Python 3.13：
#     因为 paddlepaddle-gpu 在 Windows 上只有 cp310/cp311/cp312 的 wheel，没有 cp313。
#
#  ⚠️ 装之前先确认环境变量已生效（模型缓存都落在 D 盘）：
#     HF_HOME=D:\ai-models\hf   HF_ENDPOINT=https://hf-mirror.com
# ============================================================================

# ---------- 向量化：BGE-M3 ----------
# Windows 上 PyPI 的 torch 默认就是 CUDA 版（自带 CUDA 运行时，无需另装 CUDA Toolkit）
torch>=2.4
sentence-transformers>=3.0

# ---------- OCR：PaddleOCR ----------
# 这里用 CPU 版：一页照片 1~3 秒，够用。
# 想上 GPU 要改用 paddlepaddle-gpu==2.6.2，并在系统里预装匹配的 CUDA 11.8 + cuDNN 8.6，
# 成本高收益小，故默认不启用。
paddlepaddle>=3.0
paddleocr>=3.0

# ---------- 大模型（统一走 OpenAI 兼容协议）----------
openai>=1.40

# ---------- 编排 ----------
langgraph>=0.2
