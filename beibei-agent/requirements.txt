# ============================================================================
#  背备不悲 · beibei-agent 核心依赖（M0/M1 阶段）
#  安装：  D:\conda-envs\beibei\python.exe -m pip install -r requirements.txt
#  重依赖（torch / sentence-transformers / paddleocr）见 requirements-ai.txt
# ============================================================================

# ---------- Web 框架 ----------
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
aiofiles>=23.2

# ---------- 配置与校验 ----------
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ---------- 数据库（与 Java 共用同一个 MySQL 库）----------
SQLAlchemy>=2.0
PyMySQL>=1.1
cryptography>=42.0

# ---------- 向量库 ----------
# ⚠️ 必须锁 2.5.x：对应 VM 里的 Milvus 2.5.23。
#    PyPI 上最新的 3.x 是面向 Milvus 3.0 的，装上去不兼容。
pymilvus>=2.5.4,<2.6

# ---------- HTTP 客户端（调用 LLM / ASR）----------
httpx>=0.27

# ---------- 文档解析 ----------
PyMuPDF>=1.24          # PDF
python-docx>=1.1       # Word
python-pptx>=0.6.23    # PPT
openpyxl>=3.1          # Excel

# ---------- 工具 ----------
tenacity>=8.3
